

## Plan: Add Facebook Links Alongside Instagram

Facebook will be added in three locations where Instagram currently appears:

### 1. Footer (`src/components/Footer.tsx`)
- Import `Facebook` from lucide-react
- Add a Facebook link after the Instagram link with the same styling pattern

### 2. Contact Page (`src/pages/Contact.tsx`)
- Import `Facebook` from lucide-react
- Add a new contact card for Facebook (e.g., `https://facebook.com/oyrial`) after the Instagram card

### 3. Index Page (`src/pages/Index.tsx`)
- No change needed here — the Instagram teaser section is specific to the Instagram photo grid, not a general social link area

### Facebook URL
Will use `https://facebook.com/oyrial` as placeholder — user can update if the actual page URL differs.

